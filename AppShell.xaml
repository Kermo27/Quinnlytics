<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    Shell.FlyoutBehavior="Disabled"
    Title="Quinnlytics"
    x:Class="Quinnlytics.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:local="clr-namespace:Quinnlytics"
    xmlns:views="clr-namespace:Quinnlytics.Views"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">

    <ShellContent ContentTemplate="{DataTemplate views:MainPage}" Route="MainPage" />
    <ShellContent ContentTemplate="{DataTemplate views:ConfigurationPage}" Route="ConfigurationPage" />

</Shell>
